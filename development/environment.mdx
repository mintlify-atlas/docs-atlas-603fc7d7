---
title: "App Router file-based routing and pages in Lebrijaleo"
description: "Learn how the Next.js App Router file-system routing works in Lebrijaleo and how to add new pages, layouts, metadata, and nested routes."
---

Lebrijaleo uses the Next.js App Router, where the file system defines the URL structure. Any `page.js` file you create inside `src/app/` automatically becomes a publicly accessible route — no routing configuration required.

## How file-based routing works

The App Router maps directories under `src/app/` directly to URL paths:

| File | URL |
|------|-----|
| `src/app/page.js` | `/` |
| `src/app/about/page.js` | `/about` |
| `src/app/blog/post/page.js` | `/blog/post` |

Currently Lebrijaleo has a single route: the home page at `src/app/page.js`.

## Adding a new page

To create a new route, add a `page.js` file inside a new directory under `src/app/`. Here is a minimal example for an `/about` page:

```js src/app/about/page.js
export default function About() {
  return (
    <main>
      <h1>About Lebrijaleo</h1>
      <p>This is the about page.</p>
    </main>
  );
}
```

Save the file and navigate to [http://localhost:3000/about](http://localhost:3000/about) — the route is immediately available without restarting the server.

## The root layout

`src/app/layout.js` is the root layout that wraps every page in the application. It sets up the Geist font variables, applies global CSS, and defines shared HTML structure:

```js src/app/layout.js
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({ children }) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
```

Every page is rendered as `{children}` inside this layout. Fonts and global styles are applied once here instead of per-page.

## Page metadata

You can override SEO metadata on any page by exporting a `metadata` object. This takes precedence over the root layout's metadata:

```js src/app/about/page.js
export const metadata = {
  title: "About — Lebrijaleo",
  description: "Learn more about the Lebrijaleo project.",
};

export default function About() {
  return (
    <main>
      <h1>About Lebrijaleo</h1>
    </main>
  );
}
```

## Nested layouts and special files

The App Router supports several special filenames with distinct behavior:

| File | Purpose |
|------|---------|
| `page.js` | The UI for a route segment |
| `layout.js` | Shared UI that wraps child routes |
| `loading.js` | Loading UI shown while page data fetches |
| `error.js` | Error boundary for the segment |
| `not-found.js` | 404 UI for the segment |

<Tip>
Create a `src/app/about/layout.js` to add a layout that wraps only the `/about` route and its children, without affecting the rest of the app.
</Tip>
